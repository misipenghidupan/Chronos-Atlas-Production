# This file is loaded by the 'api' service in docker-compose.prod.yml

# Django Secret Key - MUST be a long, random string

DJANGO_SECRET_KEY=a_very_long_and_secure_random_key_for_production_5264879

# CORRECTED: Point directly to the existing settings file, which will now use DJANGO_ENV to apply prod configs

DJANGO_SETTINGS_MODULE=ChronosAtlas.settings

# NEW: Tells the code that it's in a production environment, which triggers security settings in the code

DJANGO_ENV=production

# Optional: List of allowed frontend origins for CORS in production (e.g., https://prod.app.com)

CORS_ALLOWED_ORIGINS=

# --- PRODUCTION DATABASE CONFIGURATION (MUST MATCH your docker-compose file's 'db' service) ---

# The 'db' service name in the Docker network is used as the hostname.

DB_ENGINE=django.db.backends.postgresql
DB_NAME=chronosatlas
DB_USER=chronosuser
DB_PASSWORD=chronospassword
DB_HOST=db
DB_PORT=5432