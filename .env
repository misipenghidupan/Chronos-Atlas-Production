# .env - Base variables shared by both services (API and DB)

# ----------------------------------------
# DJANGO SECRETS & GENERAL CONFIG (BASE)
# ----------------------------------------
# This is a fallback value. It should be overridden by .env.prod.
SECRET_KEY='your-base-insecure-key-change-me' 
DEBUG=False

# ----------------------------------------
# DATABASE CONNECTION (SHARED)
# ----------------------------------------
# These variables define the credentials for the database service itself.
# These match the 'db' service configuration in docker-compose.yml.
DB_HOST=db
DB_PORT=5432
DB_USER=chronosuser
DB_NAME=chronosatlas
PGPASSWORD=chronospassword 

# The DATABASE_URL environment variable is only set in .env.prod 
# when connecting to an external cloud database.