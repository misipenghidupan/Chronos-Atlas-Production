# --- GENERAL APPLICATION SETTINGS ---

# Set to 'False' for production builds (default in docker-compose)

DEBUG=False

# List of hostnames or IP addresses that can access the Django server.

# For production, replace '*' with your domain name (e.g., https://www.google.com/search?q=api.chronosatlas.com).

ALLOWED_HOSTS=127.0.0.1,localhost

# --- DJANGO SECURITY SECRET ---

# MUST be unique, long, and confidential. Generate a new one for deployment.

SECRET_KEY=o-7=k-r7c1&d-d!v+v229x#1@g38e!7!k+s#+@^42j975%^7b7&y

# --- DATABASE CONFIGURATION (PostgreSQL) ---

# These variables are read by docker-compose.prod.yml

POSTGRES_DB=chronosatlas
POSTGRES_USER=chronosuser

# !!! CRITICAL: Replace with a strong, unique password !!!

POSTGRES_PASSWORD=z9!t1*w3q8#y$c7%p^d9@f6&r0v

# --- CORS SETTINGS ---

# These variables control which frontend domains can access your GraphQL API.

# Replace with the domain of your actual frontend application.

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True